// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Volcengine Redis expired key scan/cleanup task (KeyScanJob). Refers to a one-time expired key scan and cleanup job initiated for the target Redis instance. You can create, query, or cancel (interrupt) the job. The task is one-time and does not support updates. Finished/Failed/Interrupted are terminal states; canceled and failed tasks are considered nonexistent resources when queried.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as bytepluscc from "@byteplus/pulumi-bytepluscc";
 *
 * const example = new bytepluscc.redis.KeyScanJob("example", {
 *     instanceId: "redis-xxxx",
 *     scanKeyNumPerSecond: 3000,
 *     timeoutMinutes: 60,
 * });
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import bytepluscc:redis/keyScanJob:KeyScanJob example "instance_id|job_id"
 * ```
 */
export class KeyScanJob extends pulumi.CustomResource {
    /**
     * Get an existing KeyScanJob resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: KeyScanJobState, opts?: pulumi.CustomResourceOptions): KeyScanJob {
        return new KeyScanJob(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'bytepluscc:redis/keyScanJob:KeyScanJob';

    /**
     * Returns true if the given object is an instance of KeyScanJob.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is KeyScanJob {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === KeyScanJob.__pulumiType;
    }

    /**
     * Number of shards analyzed.
     */
    declare public /*out*/ readonly completedCount: pulumi.Output<number>;
    /**
     * Task creation time. Format: yyyy-MM-ddTHH:mm:ssZ (UTC).
     */
    declare public /*out*/ readonly createTime: pulumi.Output<string>;
    /**
     * Task end time. Format: yyyy-MM-ddTHH:mm:ssZ (UTC).
     */
    declare public /*out*/ readonly finishTime: pulumi.Output<string>;
    /**
     * Instance ID. You can call the DescribeDBInstances API to query basic information for all Redis instances in the target region, including the instance ID.
     */
    declare public readonly instanceId: pulumi.Output<string>;
    /**
     * Task ID of the scan job.
     */
    declare public /*out*/ readonly jobId: pulumi.Output<string>;
    /**
     * Number of expired keys scanned per second. Value range: integer between 2000 and 100000.
     */
    declare public readonly scanKeyNumPerSecond: pulumi.Output<number>;
    /**
     * Task start time. Format: yyyy-MM-ddTHH:mm:ssZ (UTC).
     */
    declare public /*out*/ readonly startTime: pulumi.Output<string>;
    /**
     * Task status. Values: Created, Running, Finished, Failed, Interrupted.
     */
    declare public /*out*/ readonly status: pulumi.Output<string>;
    /**
     * Scan timeout. Value range: integer between 30 and 1440, unit: minutes.
     */
    declare public readonly timeoutMinutes: pulumi.Output<number>;
    /**
     * Total number of shards to analyze.
     */
    declare public /*out*/ readonly totalCount: pulumi.Output<number>;

    /**
     * Create a KeyScanJob resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: KeyScanJobArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: KeyScanJobArgs | KeyScanJobState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as KeyScanJobState | undefined;
            resourceInputs["completedCount"] = state?.completedCount;
            resourceInputs["createTime"] = state?.createTime;
            resourceInputs["finishTime"] = state?.finishTime;
            resourceInputs["instanceId"] = state?.instanceId;
            resourceInputs["jobId"] = state?.jobId;
            resourceInputs["scanKeyNumPerSecond"] = state?.scanKeyNumPerSecond;
            resourceInputs["startTime"] = state?.startTime;
            resourceInputs["status"] = state?.status;
            resourceInputs["timeoutMinutes"] = state?.timeoutMinutes;
            resourceInputs["totalCount"] = state?.totalCount;
        } else {
            const args = argsOrState as KeyScanJobArgs | undefined;
            if (args?.instanceId === undefined && !opts.urn) {
                throw new Error("Missing required property 'instanceId'");
            }
            if (args?.scanKeyNumPerSecond === undefined && !opts.urn) {
                throw new Error("Missing required property 'scanKeyNumPerSecond'");
            }
            if (args?.timeoutMinutes === undefined && !opts.urn) {
                throw new Error("Missing required property 'timeoutMinutes'");
            }
            resourceInputs["instanceId"] = args?.instanceId;
            resourceInputs["scanKeyNumPerSecond"] = args?.scanKeyNumPerSecond;
            resourceInputs["timeoutMinutes"] = args?.timeoutMinutes;
            resourceInputs["completedCount"] = undefined /*out*/;
            resourceInputs["createTime"] = undefined /*out*/;
            resourceInputs["finishTime"] = undefined /*out*/;
            resourceInputs["jobId"] = undefined /*out*/;
            resourceInputs["startTime"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
            resourceInputs["totalCount"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(KeyScanJob.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering KeyScanJob resources.
 */
export interface KeyScanJobState {
    /**
     * Number of shards analyzed.
     */
    completedCount?: pulumi.Input<number | undefined>;
    /**
     * Task creation time. Format: yyyy-MM-ddTHH:mm:ssZ (UTC).
     */
    createTime?: pulumi.Input<string | undefined>;
    /**
     * Task end time. Format: yyyy-MM-ddTHH:mm:ssZ (UTC).
     */
    finishTime?: pulumi.Input<string | undefined>;
    /**
     * Instance ID. You can call the DescribeDBInstances API to query basic information for all Redis instances in the target region, including the instance ID.
     */
    instanceId?: pulumi.Input<string | undefined>;
    /**
     * Task ID of the scan job.
     */
    jobId?: pulumi.Input<string | undefined>;
    /**
     * Number of expired keys scanned per second. Value range: integer between 2000 and 100000.
     */
    scanKeyNumPerSecond?: pulumi.Input<number | undefined>;
    /**
     * Task start time. Format: yyyy-MM-ddTHH:mm:ssZ (UTC).
     */
    startTime?: pulumi.Input<string | undefined>;
    /**
     * Task status. Values: Created, Running, Finished, Failed, Interrupted.
     */
    status?: pulumi.Input<string | undefined>;
    /**
     * Scan timeout. Value range: integer between 30 and 1440, unit: minutes.
     */
    timeoutMinutes?: pulumi.Input<number | undefined>;
    /**
     * Total number of shards to analyze.
     */
    totalCount?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a KeyScanJob resource.
 */
export interface KeyScanJobArgs {
    /**
     * Instance ID. You can call the DescribeDBInstances API to query basic information for all Redis instances in the target region, including the instance ID.
     */
    instanceId: pulumi.Input<string>;
    /**
     * Number of expired keys scanned per second. Value range: integer between 2000 and 100000.
     */
    scanKeyNumPerSecond: pulumi.Input<number>;
    /**
     * Scan timeout. Value range: integer between 30 and 1440, unit: minutes.
     */
    timeoutMinutes: pulumi.Input<number>;
}
